// soma-sense — thin re-export of soma-heart/sense
// The sensorium code lives in soma-heart. This package exists so
// consumers who only want observation can `npm install soma-sense`
// without knowing about the heart internals.
export * from "soma-heart/sense";
